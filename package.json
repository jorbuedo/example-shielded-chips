{
  "name": "example-shielded-chips",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "compile": "compact compile contract/chips.compact contract/managed/chips",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "ci": "npm run compile && npm run typecheck && npm run test"
  },
  "dependencies": {
    "@midnight-ntwrk/compact-runtime": "0.16.0",
    "@midnight-ntwrk/ledger-v8": "8.1.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
